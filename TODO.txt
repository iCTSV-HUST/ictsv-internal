DOING: zip downloading for KD

- Centralized email management/sorting for KD
- Improve security for passwordHash column in 


- Access token (JWT) → short-lived (e.g. 1 hour)
	- Stored in an HttpOnly cookie
	- Used for normal API requests

- Refresh token (DB) → long-lived (e.g. 7 days)
	- Stored in another secure cookie
	- Used to issue new access tokens
	- Deletable → allows force logout


